<h1><%= @album.title %></h1>
<h3><%= @album.artist["name"] %></h3>
<h2>Tracks:</h2>
<ul>
    <% @album.tracks["data"].each do |track| %>
        <li><%= link_to track["title"], track_path(track["id"]) %>
    <% end %>
</ul>
